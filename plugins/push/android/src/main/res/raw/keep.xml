<?xml version="1.0" encoding="utf-8"?>
<!-- The notification icon is referenced only reflectively (getIdentifier), so shrinkResources
     would otherwise strip it in release shells. -->
<resources xmlns:tools="http://schemas.android.com/tools"
    tools:keep="@drawable/lecodes_push_ic_notification" />
